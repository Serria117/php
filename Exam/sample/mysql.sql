CREATE DATABASE IF NOT EXISTS abc123;
USE abc123;

CREATE TABLE IF NOT EXISTS abc12users(
	USER_NAME VARCHAR(100),
	PASSWORD_HASH CHAR(40),
	PHONE VARCHAR(10)
)
SELECT USER_NAME,PASSWORD_HASH FROM `abc12users` 
